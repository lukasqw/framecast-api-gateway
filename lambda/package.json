{
  "name": "oficina-tech-jwt-authorizer",
  "version": "1.0.0",
  "description": "JWT authorizer for Oficina Tech API Gateway",
  "main": "index.js",
  "scripts": {
    "build": "npm install && zip -r authorizer.zip index.js node_modules/"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
