name: CI

on:
  pull_request:
    branches: [develop, main]
    paths:
      - "api-gateway/**"
      - ".github/workflows/ci.yml"
  push:
    branches: [develop]
    paths:
      - "api-gateway/**"
      - ".github/workflows/ci.yml"

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: gateway-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  WORKING_DIR: api-gateway

jobs:
  validate-terraform:
    name: Validate Terraform
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.0

      - name: Terraform Format Check
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform fmt -check -recursive

      - name: Terraform Init
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform init -backend=false

      - name: Terraform Validate
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform validate

  validate-openapi:
    name: Validate OpenAPI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install Dependencies
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          pip install pyyaml jsonschema

      - name: Build OpenAPI Spec
        working-directory: ${{ env.WORKING_DIR }}
        run: python scripts/build-openapi.py

      - name: Validate OpenAPI Spec
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          if [ ! -f openapi-spec.json ]; then
            echo "::error::OpenAPI spec not generated"
            exit 1
          fi

          # Basic JSON validation
          python -m json.tool openapi-spec.json > /dev/null
          echo "::notice::OpenAPI spec is valid JSON"

      - name: Upload OpenAPI Artifact
        uses: actions/upload-artifact@v4
        with:
          name: openapi-spec
          path: ${{ env.WORKING_DIR }}/openapi-spec.json
          retention-days: 7

  test-lambda:
    name: Test Lambda Functions
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: ${{ env.WORKING_DIR }}/lambda/package.json

      - name: Install Dependencies
        working-directory: ${{ env.WORKING_DIR }}/lambda
        run: npm ci

      - name: Run Tests
        working-directory: ${{ env.WORKING_DIR }}/lambda
        run: npm test || echo "::warning::No tests configured yet"

      - name: Install Auth Lambda Dependencies
        working-directory: ${{ env.WORKING_DIR }}/lambda/auth
        run: npm ci

      - name: Test Auth Lambda
        working-directory: ${{ env.WORKING_DIR }}/lambda/auth
        run: npm test || echo "::warning::No tests configured yet"

  lint-lambda:
    name: Lint Lambda Code
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Dependencies
        working-directory: ${{ env.WORKING_DIR }}/lambda
        run: npm ci

      - name: Run ESLint
        working-directory: ${{ env.WORKING_DIR }}/lambda
        run: npm run lint || echo "::warning::ESLint not configured"

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Audit Lambda Dependencies
        working-directory: ${{ env.WORKING_DIR }}/lambda
        run: npm audit --audit-level=moderate || true

      - name: Audit Auth Lambda Dependencies
        working-directory: ${{ env.WORKING_DIR }}/lambda/auth
        run: npm audit --audit-level=moderate || true

      - name: Run Trivy Security Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          scan-ref: ${{ env.WORKING_DIR }}
          severity: "CRITICAL,HIGH"
          exit-code: "0"
